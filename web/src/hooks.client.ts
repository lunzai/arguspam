import type { HandleClientError } from '@sveltejs/kit';

// export const handleError: HandleClientError = async ({ error, event, status, message }) => {
// 	console.log('hook client error:', status, message);
// 	return {
// 		message: message || 'An error occurred',
// 		status
// 	};
// };
